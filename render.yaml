# Render Blueprint - RisesSystem
# https://render.com/docs/blueprint-spec

services:
  - type: web
    name: risessystem
    runtime: node
    region: oregon

    buildCommand: npm install && npm run build
    startCommand: npm run start

    envVars:
      - key: NODE_VERSION
        value: 20
